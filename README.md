# roucas_cli

A small C command-line tool to manage a product stock stored in a CSV file.

## Status
WIP - see `SPEC.md` and `PLAN.md`

## Data format
CSV columns:
`id,name,category,buy_price,sell_price,stock,low_threshold`

Example file: `data/stock.csv`
